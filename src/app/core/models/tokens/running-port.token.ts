import { InjectionToken } from '@angular/core';

export const RUNNING_PORT = new InjectionToken<number>('Running port from server side');

export const THEME_CLASS = new InjectionToken<string>('Theme class');
